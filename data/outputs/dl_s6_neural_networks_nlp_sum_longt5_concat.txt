In this first lecture, the narrator introduces several approaches to natural language processing: 1) tokenization; 2) subwords; 3) character; and 4) word-embeding. These approaches are described in terms of how words can be transformed into smaller objects by using neural networks. For example, "great movie," "great plot," and "terrible waste of time." Neural networks have been used for deep learning since the early days of machine learning, but they're not as powerful today as they were when humans first started using them. In this chapter, the narrator introduces several different types of sequences that can be used to classify an object as "spread or not." For example, he shows how a price forecaster could use a sequence to predict a specific price. He also shows how they can use recurrent Neural Grids to group similar words together in order to learn which word is most likely to be associated with the given value. The first part of the chapter discusses some of the challenges of using long-term memory for learning new words. However, because of the difficulty of training these kinds of networks, it is essential that they be able to move across time without losing any information. To overcome these challenges, the network uses reinforcement learning and other techniques to reduce the number of steps required to train them.